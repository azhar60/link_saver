<%# locals: (link:) %>
<% if link.tag_list.any? %>
  <div class="flex flex-wrap gap-1.5">
    <% link.tag_list.each do |tag| %>
      <%= link_to tag, links_path(tag: tag), class: "chip" %>
    <% end %>
  </div>
<% end %>
