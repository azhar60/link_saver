<%
  status_classes = {
    "ready"      => "bg-green-100 text-green-700",
    "processing" => "bg-blue-100 text-blue-700",
    "failed"     => "bg-red-100 text-red-700",
    "pending"    => "bg-gray-100 text-gray-600"
  }
%>
<div id="<%= dom_id(link) %>" class="bg-white border border-gray-200 rounded-lg p-5 shadow-sm hover:shadow-md hover:border-gray-300 transition">
  <div class="flex items-start justify-between gap-3 mb-2">
    <h2 class="font-semibold text-lg text-gray-900 line-clamp-2">
      <%= link_to (link.title.presence || link.url), link, class: "hover:text-blue-600" %>
    </h2>
    <span class="shrink-0 text-xs font-medium px-2 py-0.5 rounded-full <%= status_classes[link.status] %>"><%= link.status %></span>
  </div>

  <p class="text-sm text-gray-500 truncate mb-3"><%= link.url %></p>

  <% if link.summary.present? %>
    <p class="text-sm text-gray-700 line-clamp-3 mb-3"><%= link.summary %></p>
  <% end %>

  <%= render "tag_chips", link: link %>
</div>
